global:
  scrape_interval: 5s       # collect metrics every 5 seconds

scrape_configs:
  - job_name: 'smartnotes-backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['192.168.1.4:8080']
        # host.docker.internal = your Mac/Windows localhost from inside Docker